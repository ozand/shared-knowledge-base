# Python
__pycache__/
*.py[cod]
*.so
.Python
*.egg-info/
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Temp files
*.tmp
*.bak
.~*

# Knowledge Base cache (local only, not git-synced)
.cache/
.cache/**/*
*.db
*.sqlite

# JSON exports for AI tools (can be regenerated)
kb-export.json
kb-snapshot.json

# Local configuration files (not git-synced)
.kb-config-local.yaml

# Agent auto-generated configuration (not git-synced)
.agent-config.local
.agent-config.local.*
.session-*
.agent-cache/

# Claude Code local configuration (team config is committed)
.claude/*.local
.claude/*.local.*
.claude/.local/

# Generated index files (auto-generated, not git-synced)
_index.yaml
_index.yaml.bak

# NOTE: Metadata files (*_meta.yaml) ARE committed to git
# They contain metadata that syncs across users

# NOTE: docs/ directory IS committed (user documentation)
# Only generated subdirectories are excluded:
docs/generated/

# NOTE: .curator IS committed (marker file for curator repositories)

# Temporary files in tmp/
tmp/*.txt
tmp/*.tmp
tmp/NUL

# Local-only configuration files
*.local
*.local.*
